/**
 * TanStack Start server adapter exports.
 *
 * Server functions, middleware, and route handlers stay app-owned. The
 * adapter only aliases Spine's framework-neutral server contract so native
 * Web Request/Response behavior is preserved.
 */

export * from '../server';
