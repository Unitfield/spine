/**
 * TanStack Start adapter exports.
 *
 * TanStack Start owns server-function and route-handler glue in the consuming
 * app. This entry point keeps the import path framework-shaped while exposing
 * the same browser-safe primitives as the framework-neutral client surface.
 */

export * from '../index';
